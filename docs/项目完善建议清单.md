# 项目完善建议清单（2026-08-06 审查）

> 依据：`docs/系统架构与设计文档.md`（v2.1 开发基准）逐项核对后的缺口清单。
> 优先级：P0 = 直接影响体验/正确性；P1 = 健壮性/维护性；P2 = 打磨。
> 状态：✅ = 已完成；⬜ = 待办。
> 进度：P0 全部完成（3/3）· P1 全部完成（3/3）· P2 完成 2/5（坐车验证/迁移表）

---

## P0-1 ｜ 3+ 人联机：端到端实测 ✅ 已完成（2026-08-06）

**现状**：项目记忆中原标记「剩余唯一大项：3+ 人联机（Net.mp 房间层改造，独立工程）」——经核查**已过时**：

- `net.js` 房间层完整：`conns` Map（房主多客人连接）、`MAX_PLAYERS=4` 容量锁定、满员拒绝（`__reject` 告知）、`send(topic, payload, {to, exclude})` 支持定向/排除广播（net.js:570-586）。
- `mpWasteland.js` 协议层完整：guest 唯一身份 `myGuestId`、host 侧 `p2s` 多队友槽、`teammates` 随快照广播、`guestsHp` 独立权威血量、`readyCount` 全员就绪才开局、`meta.conn` 转发排除发送者、`guestPeers` 断线重连替换。
- 所有 3+ 人代码**从未被 3 实例实测过**（此前仅 1v1 验证）。

**✅ 已完成**：新增 `dev-tools/_cdp-mp3.mjs`（3 个 Chrome 实例 :9222/:9223/:9224 全自动握手），实测结果全绿：
1. 三端进入同一世界 ✅
2. 快照同步三端一致（僵尸 41 / 掉落 20）✅
3. 队友槽正常：host p2s=2（两 guest）；guest 端 p2s=2（host 落 'guest1' 槽 + 另一 guest）→ 各端渲染可见另外 2 人 ✅
4. 三端 FPS 全 61fps、零长帧 ✅
5. 三端零异常、零 console.error ✅

**留待专项**：战斗击杀广播（排除发送者防回听）、`guestsHp` 独立血量（模拟咬伤）、3 人掉线重连（wrejoin）。

## P0-2 ｜ 坐车「车门全挡停靠」兜底 ✅ 已完成（2026-08-06）

**现状**：车到达最近可停处但 4 个车门方向全被障碍挡住 → `stopDrive` 失败 → 车辆留在驾驶态（游戏内提示"按 WASD 挪车"）。`car-pathfinding-test.js` 因此有 1 个原版既存失败（seed=20537967 全部用例）。

**✅ 已完成**：三层兜底（`wvehicle.js`）——
1. `autoRepark`：stopDrive 失败 → 螺旋搜索 1..8 格内「车体格可放（carCellOk 验证，不穿墙）+ 门可开」就近重停；
2. `forceEject`：8 格内仍无可停（废墟死角）→ 原地放回车、玩家/乘客弹出到最近可走格（绝不卡死车内；仅代驾订单路径，手动 F 停车语义不变）；
3. 蠕动卡死保护：`_absMin` 历史最低距离，30s 内无 ≥2 格实质改善即优雅停车（原 900s 停滞根因：倒车蠕动绕过 _revN/_gstall/_noWpT 三重保护）。
**验证**：`car-pathfinding-test.js` 由 236/237 → **237/237 全绿**（既存 seed=20537967 失败消除）；smoke 188 通过；CDP 五场景 61fps 零长帧零异常。

**建议**（原方案）：`updateChauffeurDrive` 在 `finishDriveOrder` 停车失败时，沿已走路径**自动回退重试**（最多 5 格），找最近的可开门位置停车放人。既改善"NPC 代驾送到却下不了车"的体验，又能让该测试回归全绿。
- 影响面：`wvehicle.js` stopDrive / finishDriveOrder；回归：car-pathfinding-test + CDP 坐车场景。

## P0-3 ｜ 联机协议层补单元测试 ✅ 已完成（2026-08-06） ✅ 已完成（2026-08-06）

**现状**：`mpWasteland.js` 在 smoke-test 中仅 import 级覆盖（smoke-test.js:631），握手状态机、`applyMpSnapshot` 合并、`wevt` 分发等核心同步逻辑**无断言**，只能靠真实联机间接验证。

**✅ 已完成**：合并核心抽为 `wstate.js` 纯函数 `mergeZombieList(target, snapZombies)`（survival.applyMpSnapshot 改调它，逻辑等价）；smoke-test 新增 10 条联机协议断言（smoke 188 → **198 全绿**）：
- 僵尸按 id 合并：同 id 保留本地位置（_tx/_ty 插值）、快照字段覆盖、本地表现字段保留；新 id 追加（补运行时字段）；缺失 id 移除；
- `serializeMpSnapshot`：僵尸运行时字段白名单化、dev 标志块、`cull` 裁剪（drops/effects/bullets/plants 子集生效）。

**建议**（原方案）：把可纯函数化的逻辑抽到可测位置并补断言：
- `applyMpSnapshot`：僵尸按 id 合并不丢/新 id 追加/缺失 id 移除；`cull` 边界（34 格外不出现）；`guestHp` 只处理下降与死亡；
- `applyNpcSnapshot`：保留本地主控/正操控 NPC；
- 快照 `dev` 标志回传一致性。

## P1-1 ｜ localStorage 5MB 配额风险（存档静默丢失）✅ 已完成（2026-08-06）✅ 已完成（2026-08-06）

**现状**：`storage.setStorage` 已有 catch 兜底（配额满不抛错），但**会静默跳过后续所有存档**——`sv.mods.tiles` 随探索持续增长，长局玩家可能在某次存档后悄然失去进度保护。现有测试无法覆盖此场景。

**✅ 已完成**：`flushSave` 写世界档前统计 `JSON.stringify(serializeWorld)` 字节数，超 3MB 弹一次性局内提示（`_saveWarned` 防刷屏），不干预写档；long 局玩家在配额满前获得预警（不再静默丢档）。

**建议**（剩余）：2. 世界档分块/陈旧 key 合并（低优先）：同区块连续改造可合并为区块差分；3. 存档导出/恢复入口（JSON 下载/导入），作为硬备份通道。

## P1-2 ｜ 文档去重与导航 ✅ 已完成（2026-08-06） ✅ 已完成（2026-08-06）

**现状**：`docs/ARCHITECTURE.md`（旧版，未覆盖荒原模组/联机）与新版 `docs/系统架构与设计文档.md` 并存；`docs/总框架.docx` 为更早期框架。开发者易读错版本。

**✅ 已完成**：`docs/ARCHITECTURE.md` 头部加「已废弃」头注指向权威文档；`总框架.docx` 为二进制，建议人工删除或重命名（清单已注明）。

## P1-3 ｜ wevt 单条消息容错 ✅ 已完成（2026-08-06） ✅ 已完成（2026-08-06）

**现状**：`mpWasteland.onWevt` 分发无 try/catch 隔离——单条畸形/未知结构消息（旧版本客户端、协议演进期）会中断该批次后续消息处理。

**✅ 已完成**：`onWevt` 拆出 `dispatchWevt`，入口 try/catch 隔离单条畸形消息（console.warn 记录，不中断批次），协议演进期健壮。

## P2-1 ｜ guest 坐 host 车辆场景验证（零差异）✅ 已完成（2026-08-06）✅ 已完成（2026-08-06）

**现状**：guest 端无本地 `driving` 状态时（guest 搭乘 host 的 NPC 代驾），其本地位置/下车逻辑与 host 车体渲染的同步路径未单独验证（此前双实例实测未覆盖该组合）。

**✅ 已完成**：新增 `dev-tools/_cdp-mp-car.mjs` 双实例专项——host NPC 代驾（camp 近目标）行驶中采样：guest 端 `p2.driving` 状态正常同步（渲染数据在）、host/guest 双端 61fps 零长帧零异常、订单完成下车后正常。注：游戏语义中 guest 上 host 的车 = guest 各自驾驶态（非乘客），本验证覆盖"host 开车 → guest 端同步渲染"关键链路。

## P2-2 ｜ 存档字段版本迁移清单 ✅ 已完成（2026-08-06） ✅ 已完成（2026-08-06）

**现状**：`wstate.js` 各 apply 函数对缺失字段有默认值，但新增字段的默认值散落各函数内，版本演进期无集中清单。

**✅ 已完成**：`wstate.js` 头部新增「存档字段版本迁移表」注释（horde/zombies[].id/_devInfBag/interiors/boxSearched/guarded 等 + 新增字段四规则）。

## P2-3 ｜ 调试与画质选项

### P2-3a 调试 HUD ✅ 已完成
**改动**：
- 新增 `source-code/mod-wasteland/whud.js`（零依赖模块：FPS/帧耗时/位置/昼夜/实体计数/生存状态/驾驶/联机角色；500ms DOM 节流刷新；默认关闭 → 主循环零开销）
- `wdev.js` 加调试 HUD 开关按钮 + handler（切换 `sv._devHud`） + syncState 状态 + persistDev 持久化 + init 恢复（从 profile 读 `_devHud`）
- `survival.js` loop() 调 `HUD.update(sv, now)`（关闭时一进 if 就 return，零开销） + saveNow profile 加 `_devHud`
- **顺手修复既存 bug**：wdev.js 使用 `PROFILE_KEY` 但从未定义/导入（survival.js 模块私有），`persistDev()` 一执行就 ReferenceError → dev 标志不持久化 + `reportDevFlags` 后续中断。本次在 wdev.js 定义同值常量修复。
**红线合规**：`sv._devHud` falsy → update 首行 return（主循环零开销）；纯 DOM 覆盖层（pointer-events:none 不挡操作）；`_devHud` **不进 wstate dev 块 8 字段、不进 wsync**（确认 wstate.js:383 dev 块字段仅 god/stamina/inf/ammo/oneshot/bag/dmgMul/timeScale），联机各端本地独立。
**验证**（`dev-tools/_cdp-hud.mjs`）：开/关均 60fps 满帧零长帧零异常；HUD 文本正确（FPS 60 · 16.7ms · 天3 08:40 | 位置 (0,0) · 血 100/100 饱78 水78 | 僵尸 24 · NPC 7 · 子弹 0 · 掉落 5 · 特效 0 · 植物 0）；回归 smoke 198 / full-run 0 / car-pathfinding 237 / astar-verify 全过。

### P2-3b 画质档（保留为未来项）
**建议**：开发者面板加「画质档」开关（低/中/高）：昼夜渐变开关、粒子特效数量倍率、阴影密度。便于真机低配降级与诊断重渲染开销。当前不优先（HUD 已覆盖帧率/实体数诊断需求）；未来若玩家端出现帧率波动可再加。

## P2-4 ｜ 本体（塔防对局）未纳入性能防线

**现状**：本轮全部性能优化聚焦荒原模组；本体 `multiplayer/mpGame.js` 与塔防对局的性能/联机未系统审查。

**建议**：若本体仍在更新，按同一套清单（主循环/实体上限/序列化白名单）过一遍。

## P2-5 ｜ git 备份策略 ✅ 本地快照已完成（远端待用户提供地址）

**现状**：今日仓库被重建为 root-commit（无历史）、无远端。资产风险高。

**已完成（本地层）**：
- 今日全部工作已固化进 git：`4dee015`（perf audit + mp hardening + debug HUD + docs）+ `a29c536`（并行 agent 的 BFS 预算）+ `e207c7f`（root rebuild）——工作区干净，历史完整可回溯。
- 新增 `dev-tools/backup-project.py`（Python 标准库 zip 快照，不依赖远端）：含 `.git` 历史 + 全部源码/文档/音频，排除运行时数据（saves/users.json/.workbuddy/临时产物），保留最近 5 份、自动清理更旧、完整性自校验。
- 首次快照已生成：`文字植物大战僵尸-优化版-backups/pz-wasteland-20260806-181104.zip`（415 文件，11.7MB，完整性 ✅）。

**待办（远端层，需用户提供）**：用户提供远端仓库地址（GitHub/Gitee 等）后，配置 `git remote add origin <url>` + `git push`，实现异地备份。

---

## 建议执行顺序

1. **P0-1**（3 实例联机冒烟）——纯验证，无代码风险，能确认"剩余大项"真实状态；
2. **P0-2**（车门兜底）——游戏体验改善 + 消测试失败，改动集中在 wvehicle.js；
3. **P0-3**（协议层单测）——为后续联机改动铺安全网；
4. P1-1/P1-3（配额监控、wevt 容错）——低风险健壮性；
5. P2 各项按需。
