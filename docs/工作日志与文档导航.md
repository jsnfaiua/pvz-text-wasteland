# 无尽植僵荒原 · 工作日志与文档导航

> **本文件是项目的「用户视角总览」**：①开发历程大事记；②全部文档的导航索引；
> ③根目录文件说明；④常用命令速查。AI 每日详细日志在 `.workbuddy/memory/YYYY-MM-DD.md`
> （自动维护，勿手改）；本总览按里程碑同步更新。

---

## 1. 项目速览

浏览器端**文字主题植物大战僵尸**游戏（PvZ 文字具现 + 无尽荒原模组）。
- 入口：`index.html` → `game-init.js`
- 启动：`RUN.bat`（:8000）；联机：`联机对战.bat`
- 技术：ES modules / Canvas 像素渲染 / PeerJS 点对点联机 / 确定性世界生成
- 版本：v0.2.0（2026-08-06）；开发契约见根目录 `AGENTS.md`

---

## 2. 开发大事记

### 荒原模组成型期（7 月下旬 ~ 8/3）
- 荒原模组设计落地：地形（城区/郊区/荒野/废墟）、文字侵蚀与具现系统、像素美术规范、
  字楔/字源/合成配方体系（对应 `docs/统一开放文档/`、城区生态框架、文字侵蚀设计等文档）

### 8/4 — 地形生成质量诊断
- 荒原地形生成质量全面诊断与设计定稿（`docs/系统架构与设计文档.md` 确立为唯一权威基准）

### 8/5 — 路网与驾驶大修（多轮）
- 人行道「孤儿蔓延」修复 → 批量种子验证 → 发现并修复**主干道折线 axis/line 错配**（arterialInfoAt）★
- 新建 `full-run-test.mjs` 全功能无头模拟器；24 种子大规模验证 + 层级叠加验证
- 郊区/荒野人行道缺失与公路断连修复（world.js）；随机种子压力测试
- 废墟区自动驾驶「上车被挤出障碍物」修复

### 8/6 — 联机完善 + 性能大排查 + v0.2.0
- **自动驾驶根治**：BFS 车体格 2×3→2×2 对齐手动驾驶、转弯检查移除、选点扫描 240 格（bcece70）
- **路带人行道迁移 + 驾驶 BFS 节流**（8f4d578）→ **v0.2.0 发布**（tag，无远端纯本地）
- **全面缺陷审查**：31 条清单全部闭环（数据完整性/生命周期/尸潮性能/方向决策/共享流场/雇佣持久化）
- **3+ 人联机完成**（三 Chrome 实测：三端进入/快照一致/全 61fps/零异常）
- 联机室内同步/卡顿/战斗修复（室内僵尸可见/可打、室外追击）；好友加入房间失败修复（net.js）
- **卡顿大排查 8 处修复**：存档宏任务队列、流场节流+近距化、快照 34 格裁剪、驾驶 BFS 分帧、
  特效键控 Map、NPC 坐车冻结、缺字段防御、targets 近距化 —— host 59→61fps
- **泰拉瑞亚双档**（角色档跨世界 + 世界档跟种子）+ 自动驾驶兜底三件套（autoRepark/forceEject/蠕动保护）
- 下午 git 仓库重建为 root-commit（无历史，勿依赖 git 对比基线）

### 8/7 — 视觉 + 玩法 + 工程（多线并行）
- **草地最终方案**（wgrass.js）：四方连续像素基底 + 真实草密度 + 花三修
- **「格子状线条」真凶链条 6 连修**：束中心堆积 → blit 亚像素 → 草叶瓦片亚像素 → 楼间黑带 →
  canvas CSS 非整数缩放 → **噪声瓦片 7.111 格周期（函数化根治）**
- 新档读旧档修复（seed 残留）；重生 debuff 补全（food/water/infection）
- **尸化精英僵尸 + 一条命存档延续**（死亡→尸化自己/保存世界重捏脸归来）+ 距离指引（紫菱形）
- **存档管理 UI**（角色/世界导出/删除）；荒野路网修复（走廊限制/1 圈衰减）+ 世界种子下拉
- **难度收敛两档**（正常/硬核·一条命 ×1.5）；**死亡 v2**：正常模式队友救回 + 遗物包裹（掉全部物品/部分永久消失/死亡次数代价递增）
- **捏脸系统 + 局内人物动画优化**（发型三款/表情/奔跑弹跳/侧身腿部透视修复）
- **工程契约化**：AGENTS.md 重构为 12 节强制契约 → 新功能零差异门槛（§5.1）→ 游戏开发规则 11 条（§13）

### 8/8 — 角色/战斗/特效修复 + 开发规则生效
- **新建角色进老世界修复**（行为变更）：`workshop.js` 新建角色补 `forceNew:true`，
  不再继承 profile 残留 worldSeed/seed → 新角色必然进入全新随机世界
- **恶意 NPC 可击败玩家**（行为变更）：`wnpc.js` 近战（combatThreat）与弹丸
  （updateNpcBullets）对玩家扣血下限 `Math.max(1)→Math.max(0)`，与僵尸咬（resolvePlayerHit）一致；
  原下限 1 导致「永远打不死」
- **子弹/枪口/挥砍特效位置居中**：`wgear.js` 新增 `SHOT_ORIGIN_Y=26`（手部高度），
  子弹/枪口/瞄准角度以 `sv.py-26` 为原点；`render.js` 新增 `PLAYER_BODY_MID=24`，
  玩家/NPC/联机队友挥砍特效圆心上移至身体中部；`wnpc.js`/`wzombie.js` 弹道同步修正
- **随机配色多彩**：`wlook.js` 重写 `randomLook()` 为 HSV 全色域随机（肤色/瞳色/鞋色窄域），
  扩充固定色板；新建角色捏脸以随机配色为起点（不再默认继承上次外观）
- **验证**：smoke-test（285 断言）✅、full-run（5 seed×3 轮，0 异常）✅、CHECK-MODULES ✅、
  浏览器 CDP 实测（配色多样性/子弹/枪口位置）✅；NPC 扣血由源码断言 + 运行时状态确认
  （恶意 NPC 进入 fight 状态并执行攻击），自动化降血断言受主循环状态同步/自动回血干扰

### 8/9 — 角色渲染像素瑕疵修复（行走/待机头发·鞋子杂点）
- **现象**：角色行走/待机时，头发、鞋子上出现杂色像素点，且不同配色下杂点不同
- **根因（多阶段定位，最终收敛到算法层+新素材替换）**：
  ① **算法层**：`render.js` 的 `tintSprite` 逐像素按颜色最近匹配部位再染玩家色。
  `nearestPart` 阈值 `12000`（RGB 距离≈109）过宽，把阴影/描边/渐变杂色像素也强行染成玩家色
  → 形成颗粒点
  ② **资源层**（用户反馈"还没解决"才暴露）：walk-side-f0/f2、walk-back-f0、
  walk-front-f1/f2/f3 等 sprite 中**头部撞色像素**（亮棕色 [129,83,39] 系列距 MC_PAL shoes[0]
  仅 ≈750-850）会被 `nearestPart` 判给 shoes → 染成玩家鞋色 → 形成"亮黄杂点"
  ③ **用户最终诉求**：尽量不要破坏原图，只解决杂色 + 裤缝要保留 + 行走头顶有杂色
  ④ **用户新素材（2026-08-09 21:xx）**：用户提供全新风格的角色 sprite（高细节、清晰部位色），
  直接替换原 sprite，**从源头消除设计层撞色**。同时降低 maskIsolatedPixels 阈值解决
  用户反馈的"像素点缺少"
- **修复**（行为变更，**算法层 + sprite 替换**）：
  - **sprite 替换（6 个文件）**：用用户提供的素材覆盖：
    - `sprite-front.png` ← 4.png（朝南待机）
    - `sprite-back.png` ← 2.png（朝北待机）
    - `walk-side-f0.png` ← 3.png（朝东走路 f0，朝东渲染层镜像 → 显示原图；朝西不镜像）
    - `walk-front-f2.png` ← 朝南走路 f2（迈左脚）
    - `walk-front-f3.png` ← 朝南走路 f3（迈右脚）
    - `sprite-back-idle0.png` ← 朝北待机帧动画第 0 帧
  - **算法层（render.js）**：
    - `nearestPart` 阈值 `12000→7000`
    - **新增 shoes 位置软约束**：当 `nearestPart` 判定为 `shoes` 但 `ny<0.55` 时，
      重选非 shoes 部位最近者。仅对 shoes 加约束，hair/pants/skin 不加禁区
    - `maskIsolatedPixels` 阈值 `8→4`（用户反馈"像素点缺少"——mask 过头会丢失细节）
    - tintSprite 传入 ny 给 nearestPart
- **遗留（用户需补充素材）**：仍缺 5 个 sprite 未替换（**风格不一致会导致玩家在
  不同方向/动作下出现两种风格的角色**）：
  - `sprite-side.png`（朝东/西待机）
  - `walk-back-f0/f1/f2/f3.png`（朝北走路 4 帧）
  - `walk-side-f1/f2/f3.png`（朝东走路 f1/f2/f3，朝西走路共用）
  - `walk-front-f0/f1.png`（朝南走路 f0/f1）

### 8/9 增补 2 — 朝东/西待机 sprite 替换（用户素材）+ 镜像条件修复
用户提供 `未标题-1.png`（朝东/西侧视站立图）。

**关键发现：渲染层镜像方向反了**
- 用户给的 `1.png ~ 4.png`（走路）和 `未标题-1.png`（待机）都是同一套**朝东视角**图
  （玩家面部朝画面右侧，即朝东方向）
- 旧代码 `if (dir === 'right') { ctx.scale(-1, 1); }` 朝东时镜像 → 把朝东图镜像成朝西 → 朝东视角反向
- 修复：改成 `if (dir === 'left')` 才镜像，朝东保持原图（朝东视角）
- 注意：原代码注释（render.js:1054）明确写了"side 走路 = 用户给的朝东 4 帧"，
  但镜像条件写的是 `dir === 'right'`，注释与代码不一致。**用户的素材也证实了朝东图是原图，朝西需镜像**。

**修复内容**
- `sprite-side.png` ← 用户 `未标题-1.png`（朝东/西待机，朝东原图+朝西镜像）
- render.js 第 1083 行：`if (dir === 'right')` → `if (dir === 'left')`

**遗留**
- `walk-back-f0/f1/f2/f3.png`（朝北走路 4 帧）未替换，朝北方向仍是旧 sprite
- 朝东/西走路的镜像修正同步生效（走路帧与 sprite-side.png 同源）

### 8/9 增补 3 — 完整 sprite 替换 + 鞋子内部杂色修复 + 邻域投票
用户提供完整 9 张素材（`角色素材/` 目录）：
- 待机向南.png → sprite-front.png（朝南）
- 待机向北.png → sprite-back.png（朝北）
- 待机向西.png → sprite-side.png（朝东/西共用）
- 往南走1.png / 往南走2.png → walk-front-f2/f3.png
- 往东走第一帧.png / 往东走第二帧.png / 往东走第三帧.png / 往东走第四帧.png → walk-side-f0/f1/f2/f3.png

**用户关键指令**："向东的动画就靠向西的动画来镜像"——所有 side sprite 是同一套图，朝东靠镜像。
**素材方向分析**（按脸部浅色像素位置）：
- sprite-side.png = **正脸图**（左右都有脸部），不是侧视（与游戏期望不同）
- walk-side-f0/f2 = **侧视朝东**（左脸浅色）—— 用户期望"朝西视角"，实际是"朝东视角"
- walk-side-f1/f3 = 正脸

**用户报告问题**：鞋子上仍有杂色（内部）
**根因**：sprite 中鞋底缝线/阴影是 `[26,26,26]` 深色，最近色是 `eyes[0]=[28,24,20]`（距离²=44），
`tintSprite` 第979行 `if (!part || part === 'eyes') continue;` 跳过染色 → 保留原色显示为杂色

**修复**
1. **MC_PAL.shoes 扩展暗色变体**：`[[26,26,26],[40,28,16],[50,35,22],[72,50,32]]` 加入，
   让 `[26,26,26]` 被识别为 shoes 而非 eyes → 染成玩家鞋色
2. **nearestPart 阈值 7000→12000**：恢复原值，让鞋子内部高光/缝线（距离² ~10000）也染色
   （保留 shoes 位置软约束防止头顶亮棕色被错染）
3. **新增 fixByNeighborhood 邻域投票修复**：对所有不透明像素做 8 邻域平均，
   当原 RGB 与邻域平均距离² > 15000（≈RGB 距离 122）时替换为邻域平均。
   用于去除 sprite 内部未被 nearestPart 识别的深色/杂色细节
4. **render.js 镜像条件恢复 `dir === 'left'`**（朝西镜像），与用户最新指令"向东的动画靠镜像"
   一致

**结果**
- `[26,26,26]` 在鞋区残留：3119 → 590（-81%）
- 整体漏染：4731 → 2447（-48%）

**遗留**
- sprite-side.png 实际上是**正脸图**，朝东/西方向显示的"侧视"效果不如专业侧视 sprite 自然
- `walk-back-f0/f1/f2/f3.png`（朝北走路 4 帧）仍未替换，朝北走路仍是旧 sprite

### 8/9 增补 5 — MC_PAL 重匹配 + 裤腿杂色根除 + shoes 位置硬约束

**用户最终诉求（2026-08-09 多次反馈综合）**：
- "在原图的基础上，因为有杂色嘛，删除减或者减少杂色就可以了。不要将原图给篡改"
- "不管用什么颜色都不要出现杂色，但绝对要保证原图的基础上，原图可以替换颜色"
- "原图可以替换颜色，就是不同的颜色的原图可以上色"

**根因**
- `MC_PAL` 的颜色从旧 sprite 提取，与用户新素材严重不匹配：
  - shirt MC_PAL `[40,80,40]`（中绿），新素材 shirt `[18,43,26]`（深绿）
  - shoes MC_PAL `[104,72,40]`，新素材 shoes `[109,66,32]`、`[39,25,15]`
- 新 sprite 散布在裤子/衣上的**棕色杂色像素**（sprite 设计层的脏像素）距离 MC_PAL.shoes 很近 →
  被判给 shoes → 染成玩家鞋色 → 出现"裤子上闪棕色杂色"（用户反馈）
- "鞋底变黑"是 hair 主色 [55,36,21] 与鞋底暗棕 [39,25,15] 接近导致

**修复（保留原图不动，只改进算法）**
1. **MC_PAL 从用户素材实测重提取**：skin/hair/shirt/pants/shoes 全部用新素材实测主色
   （用户 2026-08-09 提供的 sprite-front/side/back + walk 帧的像素分析）
2. **新增 `nearestPalRef(r,g,b)`**：返回像素最近匹配的 MC_PAL 参考色，作为亮度缩放基准
   替代原 `MC_PAL[part][0]` 固定基准——保留原图明暗层次
3. **nearestPart shoes 位置硬约束**：`if (best === 'shoes' && ny < 0.78) return null`
   非鞋区的棕色像素不染色（保留原色 = sprite 设计杂色），消除"裤子上闪鞋色"
4. **nearestPart 底部 hair→shoes**：`if (best === 'hair' && ny > 0.80) return 'shoes'`
   鞋底暗棕变体不被判给头发
5. **保留 `maskIsolatedPixels`**：mask孤立小块（<4像素），但不动 fixByNeighborhood（已撤掉）

**验证**
- smoke-test 285 通过
- CDP 按 d/a/s/w 真实行走截图：所有方向都干净，无裤腿棕色杂色，鞋子棕色+深色鞋底

**遗留**
- 用户素材的 sprite 中确有散布的棕色像素（sprite 设计层），算法层不染色处理，
  但这些原色保留后在不同配色下可能略有视觉痕迹（非染色的"原色小点"），属用户素材层
- `walk-back 4 帧`（朝北走路）仍未替换

**教训**：核心问题不是 tint 算法，而是 MC_PAL 颜色与用户素材不匹配。
更新 MC_PAL 匹配实际素材后，基础判定就准确了，再加精确的位置约束就能根除杂色。

### 8/9 增补 — 用户新 sprite 替换 + mask 阈值调整（治本）
用户报告：
- "角色的像素点还是缺少了" → maskIsolatedPixels 阈值过高，过度 mask 丢失细节
- "头发上没有额外杂色，但是裤子上以及脚上还有" → 旧 sprite 设计层撞色
- "要不先修复一下原来的角色像素" → 用户提供全新风格 sprite，要求替换

新增修复（紧接着上一轮）：
1. **maskIsolatedPixels 阈值 `8→4`**：放宽 mask 范围，避免 mask 过头丢失细节（如眼睛、扣子等小元素）
2. **sprite 替换（6 个文件，按用户素材命名约定）**：
   - `sprite-front.png` ← 用户 4.png（朝南待机）
   - `sprite-back.png` ← 用户 2.png（朝北待机）
   - `walk-side-f0.png` ← 用户 3.png（朝东走路 f0，朝东渲染层镜像显示原图；朝西不镜像）
   - `walk-front-f2.png` ← 用户 d364a...png（朝南走路 f2）
   - `walk-front-f3.png` ← 用户 5d51...png（朝南走路 f3）
   - `sprite-back-idle0.png` ← 用户 6f84...png（朝北待机帧动画 0 帧）

**用户需补充的素材**（5 个文件，仍用旧 sprite）：

| 缺失文件 | 对应功能 |
|---|---|
| `sprite-side.png` | 朝东/西待机 |
| `walk-back-f0/f1/f2/f3.png` | 朝北走路 4 帧 |
| `walk-side-f1/f2/f3.png` | 朝东走路 f1/f2/f3（朝西走路共用） |
| `walk-front-f0/f1.png` | 朝南走路 f0/f1 |

用户补齐后按相同流程（`dev-tools/_copy-sprites.js` 一次性脚本）覆盖即可。

---

### 8/9 增补 6 — 袖口杂色 + 侧面头顶"染发"杂色根除

**用户报告**：
1. "衣服上还是有一点点，好像是袖口和衣服贴着的那个位置，会出现一点点杂色"
2. "头发正面是正常的，但是侧面就上方有一个色差特别大的一个杂色吧，就像染发了一样"

**根因**：
- 袖口：sprite 衣服区（ny 0.35-0.6）有 `[30,50,69]` 等**衣服阴影色**（蓝灰），距 pants `[28,48,66]` d²≈40 → 染成玩家裤色 → 衣服上出现蓝色块
- 侧面头顶"染发"：sprite-side 头顶有 9900+ 像素 `[126,82,43]`（**头发高光色**），距 shoes `[109,66,32]` d²=666，**比距 hair `[100,66,31]` d²=1076 更近** → 判给 shoes → 黑色头发染成棕块（"染发"）

**修复**：
1. MC_PAL.hair 加入 `[126,82,43]/[120,81,42]`（侧视 sprite 头发高光变体）→ 头发高光判给 hair → 染成玩家发色
2. nearestPart 加位置约束：`best === 'pants' && ny 0.28-0.55 → 'shirt'`（衣服区裤子色阴影改判 shirt）

**验证**：CDP 随机配色 east/west/south 截图 — 袖口干净、头顶无"染发"块、smoke-test 285 通过

---

### 8/9 增补 7 — 头发完整无"头皮"（移除 maskIsolatedPixels）

**用户报告**：
1. "现在头上的杂色已经修掉了" ✓
2. "头上那个头皮的感觉露出来了，就是头发走着走着，出来了，我想要完整的头发"
3. "就是肉色的像素点也不多"

**根因**：
`maskIsolatedPixels`（CC 连通分量 <4像素 mask 透明）原本用于去除 sprite 边缘"飞起来的脏块"，
但副作用严重——头发中的细发丝（1-3px 宽的独立连通块）也被 mask 成 alpha=0，
渲染时露出下面的肉色像素 → "头皮"。

**修复**：
- tintSprite 内**移除 maskIsolatedPixels 调用**
- 删掉整个 `maskIsolatedPixels` 函数（无引用）
- 头顶杂色已由 `nearestPart` 的"头发区域色相硬约束"（位置 + R>G>B 色相）
  彻底解决：顶部棕色像素被强制判给 hair → 染成玩家发色，不再需要 mask

**验证**：CDP 5 种发色（黑/金/绿/紫/红）截图 — 头顶完整无肉色像素点，smoke-test 285 通过

**教训**：mask 孤立块算法容易误伤正常细节。当杂色根因被定位（颜色匹配错误）
并用更精确的色相+位置约束解决后，原 mask 算法就可移除。

---

### 8/9 增补 8 — 头部杂色残余根除 + 素材不对称问题确认

**用户报告**：
1. "头发上有棕色，还有一点散落的色块，像棕色、肉色" → 头部残余杂色
2. "角色脖子上好像有杂色，手和衣服袖口叠起来的位置也有杂色" → 脖子/袖口杂色
3. "往南走裤腿右边下方有杂色，和左腿不太一样；头不对称，像缺了个耳朵" → 朝南走路帧素材问题

**根因**
1. 头部残余棕色：sprite 头顶深棕发丝 `[70,41,14]/[72,43,13]`（b=14/13）漏过旧色相约束
   （原 b>=15），被判给 shoes → 被 `ny<0.78→null` 挡住 → 保留原棕（"棕色散块"）
2. 头部绿色块：sprite-side/walk-side 头部有 `[42,78,38]` 等绿色阴影像素，距 MC_PAL.shirt
   d²=2185 较近 → 染成玩家绿衣色 → 头发位置出现绿色块
3. 裤腿/耳朵不对称：**用户提供的 walk-front-f2/f3 素材本身左右不对称**
   （右裤腿 `[15,27,39]` 深阴影 7755 像素、头部右侧多 4567 像素）——这是素材设计问题，
   tint 算法只能忠实染色不能修正形状

**修复**
1. 头发色相约束 b 下限 15→8：深棕发丝 `[70,41,14]` 也能判给 hair → 染成玩家发色
2. `best === 'shoes' && ny < 0.42 → 'hair'`：头部任何鞋色像素强制改判 hair
3. `best === 'shirt' && ny < 0.42 → 'hair'`：头部任何绿色像素强制改判 hair
   （躯干绿衣在 ny 0.35-0.65，头部不可能有绿衣）

**验证**：smoke-test 285 通过、full-run 0 异常

**遗留（素材层问题，算法无法修复）**：
- 朝南走路帧 walk-front-f2/f3 左右不对称（右裤腿阴影/头部轮廓差异）——
  这是用户提供的"往南走1/2.png"素材本身的问题，需重新提供左右对称的朝南走路帧
- 脖子/袖口交界处的杂色：系素材中该区域像素颜色分布自然过渡（肤色↔衣色），
  算法染色后交界处存在明暗过渡，属素材绘制效果

---

### 8/9 增补 9 — 发色影响上衣修复 + 捏脸界面四方向行走动画预览

**用户报告**：
1. "发色会影响上衣的上半颜色了" → 上衣上半被误染成发色
2. "脖子处好像有像素点，衣服的像素点" → 脖子/衣领交界杂色
3. "捏脸系统界面能够很清楚地看到角色的瑕疵，统就放四格动画男主的各种方向行走的动画，
   这样看到局内的效果也好一点，然后循环播放的那种" → 捏脸预览改四方向行走动画

**修复**
1. **发色影响上衣上半（根因）**：上一轮 `best === 'shirt' && ny < 0.42 → 'hair'` 规则过宽——
   上衣上半（ny 0.35-0.42）被误判为 hair → 发色污染上衣。
   修正为 `ny < 0.35`（只作用于真正头部，躯干上衣从 ny 0.35 开始不受影响）
2. **脖子衣领像素点**：系素材中脖子区（肤色↔衣领过渡）自然绘制，算法染色后交界明暗过渡，
   属素材效果；已通过 ① 的收紧间接缓解（不再把脖子区上衣改判发色）
3. **捏脸界面四方向行走动画预览（功能增强）**：
   - `wlook.js` 的 `_thumbSprites` 从"仅 front 单帧"扩展为 front/side/back 各 4 帧 walk 帧
   - `drawPreview` 改为 `requestAnimationFrame` 循环：朝南→朝东→朝北→朝西，
     每方向 4 帧（160ms/帧），循环播放（12 帧一个周期）
   - 朝东方向（dirIdx===1）做水平镜像，与局内 `dir==='right'` 镜像逻辑一致，
     保证预览与局内视觉一致
   - 帧缓存：walk-front 只引用 f2/f3 交替（与 render.js WALK_FRAME_FILES.front 一致）

**验证**：smoke-test 285 通过、full-run 0 异常、CDP 实测捏脸界面 6 帧截图
（朝南 f2→f3→朝东→朝北→朝西）确认动画循环播放、四方向行走姿态正确

---

### 8/9 增补 10 — 眼睛被发色污染修复（MC_PAL.eyes 加变体 + 头部规则收紧到 0.30）

**用户报告**：
1. "衣服的上半身稍微减弱了一点" → 上半（ny 0.35-0.42）仍有头发污染
2. "袖子横截着一半，都基本上有被头发影响" → 袖子上半被头发色污染
3. "往南走的时候，这个眼睛就是头发的颜色" → 眼睛被染成发色
4. "东往西也是挡住眼睛，眼睛感觉就是一下变成头发的颜色，一下是眼睛的颜色" →
   走路帧交替时眼睛闪烁

**根因**
- 头发色相约束 `ny<0.42` 过宽：覆盖到眼睛（0.28-0.38）和袖子上半（0.35-0.45）
- 眼睛周边深色阴影（如 [50,35,25]）距 hair[0]=[55,36,21] 仅 d²=41，被判给 hair
  → 染成玩家发色 → "眼睛变头发颜色"
- 发色像素诊断（2026-08-09）：真实头发集中在 0-30%，30-50% 的"发色像素"实际是
  眼睛/袖子上半被误判

**修复**
1. **MC_PAL.eyes 加变体 [50,35,25]/[45,30,22]**（眼窝/脸部深色阴影）→
   这些像素判给 eyes 保留深色，不再被判给 hair
2. **头部规则全部收紧到 ny<0.30**（从 0.42/0.35）：
   - 头发色相约束 ny<0.30：只作用于真正头发带（0-30%）
   - shoes→hair 收紧到 ny<0.30
   - shirt→hair 收紧到 ny<0.30
   效果：眼睛（0.28-0.38）、袖子上半（0.35-0.45）不再被头发色相约束误判
3. 眼睛保留：眼睛色 [28,24,20] 保持不变（最近 eyes 变体，保留深色原色）

**验证**：smoke-test 285 通过；CDP 金色发色（#e8b85a）朝南走 f2/f3 + 朝东走截图：
眼睛保持深色（不被金色覆盖），袖子保持肤色（不被金色污染）

---

### 8/9 增补 11 — 衣领/脸颊/眼睛仍被发色污染根本性修复（MC_PAL 根因）

**用户报告**：
1. "衣服，脖子到肩膀的位置还是受到头发的影响" → 衣领变发色
2. "头发还会影响到眼睛" → 眼睛闪烁（一帧头发色一帧眼睛色）

**根因（精确距离计算）**：
诊断发现脸颊阴影 `[117,75,50]` 到各部位的最近距离：
- hair `[120,81,42]`：**距离仅 10**（几乎重合）
- hair `[126,82,43]`：距离 13
- skin `[200,160,120]`：距离 138

→ 脸颊/眼窝阴影**被 MC_PAL.hair 的亮棕变体吸走**，整个脸部被染成发色。
眼睛 `[28,24,20]` 距 hair `[55,36,21]` 也仅 30 → 眼睛被判给 hair 染成发色。

**`shirt→hair`（ny<0.30）规则同样问题**：脖子衣领（ny 0.25-0.30 的衣服色像素
如 `[43,84,40]`/`[18,43,24]`）被判给 hair → 衣领变发色。

**修复（根因级）**：
1. MC_PAL.hair 移除 `[120,81,42]/[126,82,43]` 两个亮棕变体——它们与脸颊阴影
   距离仅 10-13，是吸走脸颊/眼窝的真凶。侧视头顶亮棕发丝改由"色相约束"
   （R>G>B 暖棕范围）覆盖。
2. MC_PAL.skin 加暗色变体 `[117,75,50]/[120,78,53]`（脸颊/眼窝阴影）→
   这些阴影判给 skin 染成玩家肤色的暗部。
3. 删除 `shirt→hair` 规则——衣领（ny 0.25-0.30 的衣服色）确实属于衣服，
   不该改判头发。
4. `shoes→hair` 收紧到 ny<0.25——脖子区不再被改判头发。

**验证**：smoke-test 285 通过；CDP 金色发色朝南走 f2/f3 + 朝东走 + 朝西走截图：
眼睛保持深色、脖子衣领保持绿色衣服、不闪烁。

---

### 8/9 增补 12 — 眼睛边缘"金色边框"诊断（素材层确认，停止算法修改）

**用户报告**："眼睛还是有点怪怪的，你对照着我原图看一下，有点杂色"

**对照原图诊断**（face-sprite-front.png 等脸部放大对比图）：
- 眼睛本身：深色（保留原色）✓ 正确
- 眼睛**上方/眉毛**：原图是棕色头发（玩家棕色发），tint 后染成玩家金色 → 正常
- 眼睛**周围金色颗粒**：诊断 walk-front-f2/f3 头部 ny<0.30 区域 13-14万 像素被判给金色发色，
  原色 `[100,66,31]`/`[100,65,33]`/`[55,37,17]` 等——**这些是真实的头发像素**
  （不是杂色），距 hair 主色最近 → 判给 hair 正确

**核心结论**：眼睛"怪怪的"是 **sprite 素材中头发延伸到眼睛附近**（眉毛/发际线），
算法正确染色（这些像素就是头发），**不是 tint 算法的问题**。

**停止算法修改**——继续改 nearestPart 会陷入"修了 A 坏了 B"的循环。
眼睛周围的金色像素**是头发，染成发色正确**。

**遗留**：
- 眼睛"周围金色边框"是**素材设计**问题（头发覆盖到眼睛区域边缘）——需用户提供
  头发不延伸到眼睛附近的 walk-front 帧，或接受现状
- 同样**朝南走路帧 walk-front-f2/f3 左右不对称**也是素材问题（已在 8/9 增补 8 记录）

**教训**：诊断 tint 算法问题时，需**对照原图**——若 tint 后颜色与原图对应位置
的"语义"一致（眉毛是头发→染成发色正确），则不是算法 bug。停止过度修改。

### 8/9 增补 13 — NPC 动画复用 + 名牌隐藏/去阵营色 + 刷新规则 + 昏迷苏醒
用户需求："NPC 待机/行走动画与玩家几乎没差别可复用；NPC 名字放头顶、走近才显示、
不分阵营颜色；出生点/营地不刷新 NPC（营地生育保留）；进游戏黑灰眨眼昏迷苏醒；
设计僵尸渲染视觉效果放临时浏览器预览。"

- **NPC 动画复用玩家**：`render.js` `drawNpcs` 按本帧位移 `n.x-n._lastAX` 判断"在走"，
  朝向随之切换，组 `anim{dir,moving,frame}` 传给 `drawPixelPlayerBody`——复用玩家待机/
  行走精灵（原本 NPC 只走待机动画，现在会走动）。室内躲藏幸存者仍静态待机。
- **名牌隐藏 + 去阵营色**：`drawNpcs`/`drawInterior` 名牌改为中性浅色 `#D6DDE6`，
  不再按 party/hostile/friendly/neutral 染色或发光；只有玩家走近（距离 <2.2×TS ≈
  可交互距离）才显示名字，血条也只在走近时显示。友善/中立/敌对远近看不出区别。
- **刷新规则**：`wnpc.js` `ageNpcs` 删除"营地每日来新居民"刷新（`CAMP_NEWCOMER_*`
  常量一并移除，保留**营地生育**）；野外随机刷新避开出生点 `SPAWN` 14 格内。
- **昏迷苏醒**：新游戏首次进入（`startRun` 内 `playT<=0 && day<=1`）设 `sv._wake`；
  `waction.js` `moveInput` 苏醒期间返回 0（锁移动）；`render.js` `drawWakeOverlay`
  做黑灰眨眼（峰值暗度 0.72→0.38→0）+ 浮现"你在荒野中醒来…"；计时在
  `update`/`updateGuest` 推进，期满清空。
- **僵尸视觉预览**：新建 `dev-tools/_qa_tmp/_zombie-preview.html`（自包含、复用游戏内
  28×36 像素画法），6 套调色板 × 5 种僵尸 × 3 档感染对比，临时预览确认后固化/清理。

**验证**：smoke-test 292/292；full-run-test 5 seed 0 问题；CHECK-MODULES 全部 OK。

### 8/9 增补 14 — 室内动画与室外同步 + 室内交互光圈
用户反馈：① 室内和室外动画没同步（室内走动时小人卡在原地）；② 室内搜索箱子没有
室外那种"靠近箱子边缘的黄色光圈"提示。

- **室内动画与室外同步**：`survival.js` `updateInteriorMode` 的室内移动块原本只推
  `stepT/stepSide` 播放脚步音效，但**没推进 `animFrame`**，也没设 `animMoving`/朝向
  → 室内走动动画帧冻结。补上与室外大世界移动一致的：`sv.animMoving` 置真/置假、
  `sv.faceX/faceY` 朝向、`isSide` + `animFrame` 推进（guest 端同走 `updateInteriorMode`，
  保持单机/联机一致）。
- **室内交互光圈**：新增 `updateInteriorPrompt()`（判定与 `doInteriorInteract` 完全一致：
  先最近躲藏幸存者 ≤1.9×TS，再 3×3 内第一个箱子），结果存 `it.promptTarget`；
  `render.js` `drawInteriorPromptGlow` 在玩家下方画金色呼吸光圈——箱子用金色描边矩形
  （与室外交互框同款 #FFD700），幸存者用金色圆圈。确定 F 会作用于哪个对象。

**验证**：smoke-test 292/292；full-run-test 5 seed 0 问题（floors 15 覆盖室内路径）。

### 8/9 增补 15 — 角色动画帧 → 僵尸「残损」效果实验（预览未固化）
用户需求："试试把角色的动画帧装到僵尸上是什么效果；不要和角色一模一样，要有残损
的感觉，僵尸要有僵尸的样子。"

- 做法：**不改游戏逻辑**，新建自包含预览页 `source-code/_zombie-anim-preview.html`
  （临时，验收后清理）。加载玩家行走/待机帧（walk/sprite-front/side/back），对每帧做
  僵尸化变换并循环播放，左侧玩家原帧、右侧僵尸化帧对比：
  - `cropContent`：先裁掉 sprite 大片透明留白，让角色变大。
  - `nearestPart`（复用 render.js MC_PAL 部位分类）：皮肤→腐绿、头发→枯色、衣服→脏衣、
    裤子→泥色、鞋→黑鞋。
  - `rotColor` 兜底：未分类像素一律去饱和 + 偏绿灰 + 随机脓绿/血斑/苍白腐肉斑。
  - 残损打洞：按 hash 随机镂空躯干像素，越靠边缘越容易烂出缺口。
  - 高感染浮现"尸"字；驼背压低（上部 65% 下移 hunch 像素）。
  - 控制条：残损度 0-100、感染 0-100、方向、动画开关。
- 启动 `server.js`（含 source-code 静态服务 + PeerServer），页面
  `http://localhost:8000/source-code/_zombie-anim-preview.html`。

**待用户确认**风格后再固化进 `render.js` 的僵尸绘制；确认后删除临时预览文件。

### 8/9 增补 16 — 修复 NPC 动画：行走/待机真正动起来 + 名牌放图片上方
用户反馈：① NPC 动画有问题，只在出生一瞬间走一下，后续走路是"平移"；没有待机动画，
也没有行走动画；② NPC 名字应在渲染图片上方（之前压在小人身上）。

- **根因**：渲染层原来用"本帧位移差 dx/dy"判断 NPC 是否在走。但远离玩家的 NPC AI
  每 0.3s 才 moveToward 跳一格，两次跳之间 n.x 不变 → 渲染判定"没在走"→ 只有跳的那
  一帧闪一下走动画，其余全是静止 → 视觉上是平移/只在出生瞬间走一下。走路帧推进又依赖
  位移，导致帧冻结 → 待机/行走动画都不动。
- **修复**（`wnpc.js` + `render.js`）：
  - `moveToward`：置 sticky 标记 `n._moving=true` 并记录朝向 `n._faceX/_faceY`；
    `updateNpc` 每次 tick 开头复位 `n._moving=false`（停留/休息/打工时不移动 → 待机）。
    远离玩家 NPC 在 0.3s 间隔内 `_moving` 保持 true → 行走动画连续，不再平移。
  - `drawNpcs`：用 `n._moving` 判移动、`_faceX/_faceY` 判方向；走路帧按 `performance.now()`
    现实时间推进（10fps），与位移解耦；`moving=false` → 走待机动画。
  - 名牌位置：新增 `npcNameplateY(n)`——用角色精灵"内容 bbox 顶边"（缩放后）定位名牌，
    让名牌悬浮在小人上方，不再压到身上（sprite 含大量透明留白，按整图算会错位）。
    `drawNpcs` 与 `drawInterior` 的 NPC 名牌统一用它。

**验证**：smoke-test 292/292；full-run-test 5 seed 0 问题；CHECK-MODULES 全部 OK。

### 8/9 增补 17 — NPC 背包 UI 关不掉修复 + 睁眼效果渐变 + 苏醒无敌
用户需求：① 打开 NPC 物品栏查看物品后 UI 关不掉；② 睁眼效果改成"从模模糊糊昏暗到
明亮清晰"的渐变；③ 出生点不刷新 NPC；④ 开局眨眼动画时角色无敌、状态不减少不变。

- **NPC 物品栏 UI 关不掉**：`survival.js` keydown 里 B 键无条件 `Panel.toggleBag`，而
  界面提示是"B/F/ESC 关闭"。储物柜（含 NPC 只读查看）打开时按 B 只会切背包、储物柜关
  不掉。改为：`Panel.isChestOpen()` 时 B 先 `hideChest()`。
- **睁眼效果渐变**：`render.js` `drawWakeOverlay` 从"黑灰眨眼(中途变暗)"改为**单调变亮**
  （easeOutCubic，暗度 1→0 无中间变暗）+ 开场视线 blur(5px→0) + 灰雾(0.5→0)，营造
  "模模糊糊昏暗→明亮清晰"的睁眼感；苏醒文字前段浮现、结尾淡出。
- **出生点不刷新 NPC**：`wnpc.js` 野外随机刷新已避开 `SPAWN` 14 格内（此前已实现，确认
  保留）。
- **苏醒无敌 + 状态冻结**：新增 `wakeActive(sv)`（`sv._wake` 进行中）；`survival.js`
  `update`/`updateGuest` 的饱食/水分/体力/回血整块在苏醒期跳过（状态不变）；僵尸咬
  `resolvePlayerBiteTick`、恶意 NPC 近战/弹丸、患病掉血、食人花、植物弹丸、车祸等在
  苏醒期全部免伤（waction/wnpc/wplants/wvehicle/survival）。

**验证**：smoke-test 292/292；full-run-test 5 seed 0 问题；CHECK-MODULES 全部 OK。

### 8/9 增补 18 — 修复僵尸动画预览页黑屏
用户反馈：僵尸设计预览效果看不到，黑屏没效果。

- **根因**：`source-code/_zombie-anim-preview.html` 里 `fitDraw(cv, src)` 第一个参数应为
  **canvas 元素**（内部 `cv.getContext('2d')`），但 `drawAll` 调用时传的是 `px()` 返回的
  **context**（`walkPC/walkZC/idlePC/idleZC`），导致 `cv.getContext is not a function`
  每帧抛异常 → 整个渲染中断，canvas 只有深色背景 → 黑屏无效果。
- **修复**：`drawAll` 改为传 `document.getElementById('walkP'/'walkZ'/'idleP'/'idleZ')`
  给 `fitDraw`；删除未使用的 `walkPC` 等 context 变量。
- **验证**：CDP 诊断确认 `drawAll => OK`、21 个异常清零；4 个 canvas 均有实际彩色像素
  （玩家/僵尸都被绘制，僵尸偏腐绿色调）。页面重新打开可正常查看。

### 8/9 增补 19 — 僵尸多彩渲染恢复 + 室内外 NPC 逻辑统一 + 出生点 NPC 精简
用户需求：① 僵尸恢复"之前那种很多种类、颜色好看"的渲染；② 室内外 NPC 交互 UI 应一致；
③ 室内 NPC 也要能动、可招募/邀请；④ 招募后跟随进出室内外；⑤ 出生点为什么刷很多 NPC。

- **僵尸多彩渲染**（`render.js`）：新增 `ZOMBIE_PALETTE` 按类型差异化配色——
  normal/cone/bucket/door/flag/pole/remnant/deleter/swapper/giant 各有专属皮肤+头发色板，
  外衣沿用各类型 def.color；playerzombie 保留继承肤色。多种僵尸一眼可辨且协调好看。
- **室内外 NPC 统一**：
  - 交互 UI：`renderNpcMenu` 删除 `isInteriorNpc` 特判——躲藏幸存者与室外 NPC 同一套菜单
    （查看/交易/邀请/雇佣）。
  - 招募/雇佣室内躲藏者：新增 `adoptInteriorNpc`（`survival.js`）——从 `it.npcs` 移入
    `sv.npcs`（统一列表），标记 `inInterior`；`invite`/`hire` 时调用。
  - 跟随进出：`windoor.js` `enterFollowers`——进室内时室外 party+follow 队员标 `inInterior`
    带入室内；`exitInterior`——把 inInterior 的 party 队员带回门口外；`changeFloor` 换楼层
    时队员重新定位到新楼层出生点旁。
  - 室内跟随驱动：`updateInteriorMode` 对 inInterior 且 party 的队员用 `moveToward`（室内
    canStand）跟随玩家；躲藏者保持 hide 不动。
  - 渲染：`drawInterior` 画 `sv.npcs` 中 inInterior 队员；室外 `drawNpcs` 跳过 inInterior。
  - 交互/光圈：`doInteriorInteract`/`updateInteriorPrompt` 支持 inInterior 队员。
  - 存档：`serializeNpcs` 补 `inInterior/interiorKey/interiorFloor` 字段（restore 自动带上）。
- **出生点 NPC 精简**（`wnpc.js` `spawnInitialNpcs`）：友善 2→1 名；恶意 4→2 名且放到
  22~30 格外（原 12~22 格），开局不再堆人在主角身边。
- **刷新概率说明**：野外每日 15%（12~30 格、避开出生点 14 格，友好/中立/恶意 45/35/20）；
  营地生育 2%/天（友善≥2）；室内高楼层 40% 概率 1~2 名躲藏者；营地"来新居民"已移除。

**验证**：smoke-test 292/292；full-run-test 5 seed 0 问题；CHECK-MODULES 全部 OK。
代码行数 35129（58 文件）。

### 8/9 增补 20 — NPC 刷新概率微调 + 出生点屏幕内不刷生物
用户需求：野外刷新概率 0.2%；营地生育 0.5%；室内 2 楼刷新概率 0.15%、每高一层 +0.03%；
出生点屏幕内不出现敌对/友方生物。

- **概率调整**（`wbalance.js`）：
  - `WILD_NPC_DAILY_CHANCE 0.15 → 0.002`（野外每日 0.2%）
  - `BIRTH_DAILY_CHANCE 0.02 → 0.005`（营地生育 0.5%）
  - `INTERIOR_NPC_CHANCE 0.4 → 按楼层递增`：新增 `INTERIOR_NPC_CHANCE_BASE=0.0015`
    （2 楼 0.15%）、`INTERIOR_NPC_CHANCE_PER_FLOOR=0.0003`（每高一层 +0.03%）；
    `windoor.js spawnInteriorNpcs` 改为 `chance = 基准 + max(0, floor-2) × 每层增量`
- **出生点屏幕保护**：`wbalance.js` 新增 `nearSpawnScreen(spawn,x,y)`（半屏 15×9 格，
  TS=36）；`wzombie.js` 野外/僵尸群刷新、`wnpc.js` 野外 NPC 刷新、初始 NPC 均避开出生点
  屏幕——初始友善放 17~21 格（屏幕外），恶意 22~30 格。

**验证**：smoke-test 292/292；full-run-test 5 seed 0 问题；CHECK-MODULES 全部 OK。

### 8/9 增补 21 — 修复"删除所有存档后开始游戏仍显示存档痕迹"
用户反馈：删除所有存档后，点开始游戏仍出现"删档的痕迹"、无法选择。

- **根因**：开始游戏弹窗打开时，读取了可能残留的选中缓存 `ws_save_sel_world/ws_save_sel_char`
  和 `wasteland_profile`（worldSeed/characterName 组合）。若这些缓存指向**已删除**的存档
  （如工坊删除时未同步清理、或用户手动清 localStorage），弹窗会显示旧存档痕迹/异常状态。
- **修复**（`survival.js`）：新增 `cleanupStaleSaveCache()`，在 `renderStartDialog` 每次打开
  弹窗前调用——校验 `ws_save_sel_world`/`ws_save_sel_char`/`profile` 指向的存档是否存在，
  不存在则清空对应缓存字段。开始弹窗只反映真实存在的存档。
- **验证**：CDP 完整复现"造档→删档→开弹窗"：删除前显示"世界 #7777"，删除后世界列表为空、
  角色"未绑定"、按钮禁用、提示"请先选择世界"，无任何残留痕迹。

### 8/9 增补 22 — 修 indoor 模式 controllerId bug + 草优先级最低 + 草不长到人行道
用户反馈：① indoor 进入后控制台报 `controllerId is not defined`（白屏）；② 草坪上的草层级
优先级最低，其余物品放草坪上会盖过草；③ 草只长在草坪上，不长到人行道/路面上。

- **indoor bug**（`survival.js updateInteriorMode`）：之前写 `controllerId === n.id`（裸变量
  未定义），改为 `sv.controllerId && n.id === sv.controllerId`（与室外统一使用 state 字段）。
- **草优先级最低**（`render.js` 主循环）：将 `WGRASS.grassRenderLayer`（动态草层）从
  `drawWorld` 之后移到之前——静态草底由 `drawGroundTile`（最底层）画，物体（树/箱/车/僵尸/NPC/
  弹丸等）全部画在动态草层之上，草束永远不盖物品。
- **草不长到人行道**（`wgrass.js grassRenderLayer`）：束体 24×16 跨多格，新增占格 bbox 检查
  ——束中心 3×3 邻域所有格必须都是 GROUND/WEED，否则不画该束。草束严格只在草坪上生成，
  不会画到人行道/公路/建筑地块。

**验证**：smoke-test 292/292；full-run-test 5 seed 0 问题；CHECK-MODULES 全部 OK。

### 8/9 增补 23 — 修复"NPC 只读物品栏关不掉"（hideChest 引用未定义 sv）
用户反馈：和友善 NPC 交流 → 打开他的物品栏查看 → 界面关不掉（ESC/B/F 都关不了）。

- **根因**（CDP 完整复现）：`panel.js` `hideChest()` 无 `sv` 参数，且 panel.js 不持有 `sv`
  单例；但函数内写了 `sv.chestReadonly = false` → 每次 ESC/B/F 关闭储物柜都抛
  `ReferenceError: sv is not defined`，**中断在 `chestEl.classList.add('hidden')` 之前**——
  `chestOpen` 已置 false（逻辑上关了）但 DOM 层储物柜没隐藏 → 界面看起来"关不掉"。
  （此前 B 键修复只是让 B 走到 hideChest，没发现 hideChest 内部本就抛错。）
- **修复**：`hideChest()` 移除对 `sv` 的访问（只读标记由 `showChest` 每次打开时重新设置，
  关闭无需清除）。保留 `chestOpen=false / selInfo=null / classList.add('hidden')` 正常执行。
- **验证**：CDP 清缓存实测——ESC/B 关闭只读储物柜后 `chestOpen=false`、DOM hidden=true、
  **0 异常**（修复前 2 异常且 DOM 不隐藏）。

### ★ 权威基准（开发必读）
| 文档 | 用途 |
|---|---|
| `系统架构与设计文档.md` + `architecture-diagram.svg` | **唯一权威设计基准**（联机零差异清单 §5 / 性能防线 §6） |
| `AGENTS.md`（根目录） | 开发规则强制契约（红线/验证矩阵/游戏开发规则） |
| `ARCHITECTURE.md` | 模块依赖方向 |

### 荒原模组设计
| 文档 | 用途 |
|---|---|
| `统一开放文档/创意工坊-荒原模组统一开发文档-v2.52.md` | 荒原模组统一开发文档（v2.52，最新） |
| `统一开放文档/创意工坊-荒原模组统一开发文档.md` | 早期版本（v1，已被 v2.52 取代） |
| `荒原模组-城区生态与楼层框架.md` | 城区生态/室内楼层框架 |
| `荒原联机-复用评估与实施设计.md` | 联机实施设计（wstate/快照/事件） |
| `荒原模组-后续开发设计.md` | 后续开发方向 |

### 文字具现系统
| 文档 | 用途 |
|---|---|
| `文字侵蚀与现实具现系统设计.md` | 文字侵蚀机制设计 |
| `文字侵蚀与具现化模块开发状态总览.md` | 模块开发状态 |
| `全具象物清单与文字具现配方设计.md` | 具象物清单 + 配方设计 |

### 美术与实现
| 文档 | 用途 |
|---|---|
| `像素具现美术规范.md` | 像素美术规范 |
| `寻路系统设计与实现.md` | A*/流场/寻路设计 |
| `启动说明.md` | 启动方式说明 |
| `联机特效同步-修复报告.md` | 联机特效同步修复记录（根目录移入，待整理） |

### 管理
| 文档 | 用途 |
|---|---|
| `模块进度路线图.md` | 模块进度规划 |
| `项目完善建议清单.md` | 完善项清单 |
| `总框架.docx` | 早期总框架 |

---

## 4. 根目录文件说明

| 文件/目录 | 说明 |
|---|---|
| `RUN.bat` / `联机对战.bat` | 一键启动（单机 / 联机） |
| `ps-server.ps1` / `server.js` | 服务器（Node + PowerShell 备用；server.js 内置本地 PeerServer :9000） |
| `index.html` / `game-init.js` / `style.css` | 页面 / 入口 / 全局样式 |
| `net.js` | 联机网络层 |
| `package.json` / `node_modules` | 依赖（仅 peer） |
| `source-code/` | 全部源码（core/systems/entities/ui/persistence/multiplayer/mod-wasteland） |
| `assets/` | 音频等资源 |
| `docs/` | 设计文档（见 §3） |
| `dev-tools/` | 测试与回归工具（smoke/full-run/car-pathfinding/astar-verify/CDP 实测） |
| `saves/` + `users.json` | **运行时数据**（存档/用户），勿提交 |
| `AGENTS.md` / `.gitignore` | 开发契约 / git 忽略规则 |

---

## 5. 常用命令速查

| 用途 | 命令 |
|---|---|
| 启动单机 | `RUN.bat` 或 `node server.js` → http://localhost:8000 |
| 启动联机 | `联机对战.bat` |
| 冒烟回归 | `node dev-tools/smoke-test.js` |
| 全功能模拟 | `node dev-tools/full-run-test.mjs` |
| 车辆寻路 | `node dev-tools/car-pathfinding-test.js` |
| A* 校验 | `node dev-tools/astar-verify.mjs` |
| 模块检查 | `powershell -ExecutionPolicy Bypass -File dev-tools/CHECK-MODULES.ps1` |
| 浏览器实测 | `node dev-tools/_cdp-perf.mjs`（单机 FPS）/ `_cdp-mp-real.mjs`（双端联机） |
| 详细规则 | 见 `AGENTS.md` §9 验证矩阵（改什么 → 跑什么） |

---

## 6. 工作日志制度

- **每日详细日志**：`.workbuddy/memory/YYYY-MM-DD.md` —— AI 每次完成实质工作自动追加
  （含根因/修复/验证/教训），历史从 2026-08-04 起连续维护。
- **本总览**：面向用户的大事记 + 文档导航，按里程碑更新（建议每完成一个功能迭代同步一次）。
- 归档规则：日志超过 30 天按主题蒸馏进 `.workbuddy/memory/MEMORY.md`（长期项目记忆）。

## 7. 预览资源清理制度（2026-08-07 定）

- 开发中生成的预览资源（CDP 截图 `_cdp-*.png` / `_qa_tmp/` 临时文件 / 临时预览页）
  在**预览验收完成后定期清理**，至少每个功能迭代结束 / 提交前清一次。
- 仅保留三类：① docs/ 设计文档引用的图；② 标注为回归基线的对照素材；
  ③ 用户明确要求保留的效果图。
- 规则细则见 `AGENTS.md` §13.12（硬性）。

## 8. 代码行数统计与冗余清理制度（2026-08-07 定）

- **每次功能迭代完成必须统计代码行数**：`node dev-tools/count-lines.mjs`
  （总行数 + 按模块分布，mod-wasteland 按文件明细）。
- **基线（2026-08-07）**：总计 **32,099 行 / 58 文件**；mod-wasteland 21,615 行（30 文件），
  其中 render.js 4,298 / survival.js 4,177 / wnpc.js 1,597 / world.js 1,159 / wvehicle.js 1,095。
- 按功能评估行数增量，异常增长排查冗余；删除冗余须：确认无引用 → 全量回归验证 →
  游戏运行与全部功能不受影响才可提交。
- 规则细则见 `AGENTS.md` §13.13（硬性）。

## 9. 开发者模式制度（2026-08-07 定）

- **所有新功能必须提供开发者模式测试入口**（局内 F9 面板 `wdev.js` / `saveData.devMode`）：
  属性/物品/召唤/状态/时间等至少一类，无需走完整自然流程即可直接验证。
- **开发者模式对任何玩家开放**（单机 / 联机 host / 联机 guest，无权限门槛）；
  联机属性类 `devflags` 上报回传、召唤类 `devcmd` host 权威执行（双端可见）。
- 规则细则见 `AGENTS.md` §13.14（硬性）。

## 10. 开发前审计与止损纪律（2026-08-07 定）

- **开发前审计**：改代码前先读目标函数及调用方/依赖，评估影响面，禁止盲改。
- **无损改动**：禁止破坏既有功能与行为（含旧档兼容、联机零差异）；
  确需变更既有行为须先征得用户同意并标注「行为变更」。
- **及时止损**：发现改动损伤既有功能立即停止，先回滚/修复再继续，禁止带伤开发。
- **每次做完必查**：按 §9 验证矩阵跑对应验证 + 手工复查受影响流程，确认无破坏才提交。
- 规则细则见 `AGENTS.md` §11.8（硬性）。
