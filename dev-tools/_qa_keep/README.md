# _qa_keep/ — 预览资源保留区（永久保留，不参与定期清理）

规则（2026-08-07 用户约定）：
- 我（AI）生成的预览截图默认是**临时资源**，放在 `dev-tools/_qa_tmp/`，每周自动清理（见 `dev-tools/cleanup-previews.mjs`）。
- 用户看完预览后，**确认要保留的图片**移动到这个目录 `_qa_keep/`，永远不会被自动删除。
- 目录内文件人工维护：保留的图按用途命名，可自行增删。

当前保留：_（空，等待用户挑选）
