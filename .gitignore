# ============================================================
# 文字植物大战僵尸-优化版 · Git 忽略规则
# 建立于 2026-08-05（资深开发者主导 Git 化）
# ============================================================

# ---- 多会话并行开发的 .bak 备份（已被 Git 取代，勿入库） ----
*.bak
*.bak-*
*.bak-*-*

# ---- 运行时游戏存档（玩家本地进度，含隐私，勿入库） ----
saves/
saves/*.json

# ---- 用户/账户数据（含凭证） ----
users.json

# ---- 临时诊断 / 截图 / 测试产物（开发期生成） ----
dev-tools/_qa_tmp/
dev-tools/_qa_keep/*.png
dev-tools/_cdp-*.png
dev-tools/_chain-test.mjs
**/_cdp-*.png
**/*.tmp
*.tmp

# ---- 二进制音频资源（体积大，按需 LFS 或单独管理） ----
# 注：mp3 是游戏运行必需资源，如团队决定纳入版本控制可删除下方规则；
#     当前选择忽略以避免仓库膨胀（从源处同步即可）。
assets/audio/*.mp3

# ---- 系统/编辑器文件 ----
desktop.ini
Thumbs.db
.DS_Store
*.swp
*~

# ---- Node / 依赖 ----
node_modules/
npm-debug.log*
package-lock.json

# ---- 本地环境 / IDE ----
.vscode/
.idea/
*.local

# ---- WorkBuddy / CodeBuddy 工作区记忆（不入库，属本地 agent 上下文） ----
.workbuddy/
.codebuddy/
